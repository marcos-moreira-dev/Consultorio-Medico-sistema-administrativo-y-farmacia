/**
 * Gancho reservado para un bootstrap futuro de OpenTelemetry en navegador.
 * Por ahora se mantiene explícito como no-op para no fingir una integración inexistente.
 */
export function bootstrapOpenTelemetry(): void {
  // no-op intencional en esta fase del storefront
}
