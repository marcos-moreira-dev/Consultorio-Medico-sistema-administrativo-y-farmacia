# Wrapper liviano del proyecto.
# En este repositorio se deja el punto de entrada y la configuración mínima
# para que el equipo pueda invocar el backend de forma uniforme.
# Si deseas el wrapper completo oficial, ejecuta en tu máquina:
#   mvn -N wrapper:wrapper
# o:
#   .\mvnw.cmd -N wrapper:wrapper
# una vez que tengas Maven instalado.
wrapperVersion=3.3.2
